/**
 * Job extraction stub.
 *
 * In a future version this module will:
 * 1. Fetch the job posting URL.
 * 2. Parse the page using ATS-specific adapters or a generic heuristic.
 * 3. Populate title, company, location, description, and apply_url on the job row.
 *
 * For v0 this is not yet implemented — job rows start as placeholders.
 */
export function extractJob(_jobId: number): void {
  throw new Error('extractJob not yet implemented');
}
