/**
 * Generic / Stagehand adapter stub.
 *
 * Future: fallback for ambiguous application forms that don't match a known
 * ATS.  Uses Stagehand (AI-driven) to interpret and fill unknown forms.
 * Always defaults to --dry-run.
 */
export const ADAPTER_NAME = 'generic-stagehand';
