/**
 * Email classification stub.
 *
 * Future: classifies emails as recruiter outreach, OA/HackerRank, interview
 * scheduling, rejection, or follow-up using LLM or keyword heuristics.
 *
 * For v0 this is not yet implemented.
 */
export function classifyEmail(_subject: string, _body: string): string {
  throw new Error('classifyEmail not yet implemented');
}
