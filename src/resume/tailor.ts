/**
 * Resume tailoring stub.
 *
 * Future: given a scored job and the candidate profile, produce a tailored
 * resume data object that reorders/selects/lightly rephrases real experience
 * to match the job posting.  Never fabricates content.
 *
 * For v0 this is not yet implemented.
 */
export function tailorResume(_jobId: number): void {
  throw new Error('tailorResume not yet implemented');
}
