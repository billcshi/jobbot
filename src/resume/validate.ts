/**
 * Resume validation stub.
 *
 * Future: checks that a tailored resume does not fabricate any claims
 * beyond what appears in profile/candidate.yaml.
 *
 * For v0 this is not yet implemented.
 */
export function validateResume(_versionId: number): void {
  throw new Error('validateResume not yet implemented');
}
